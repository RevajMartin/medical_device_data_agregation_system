# Core dependencies
# NOTE: versions bumped to ranges that ship prebuilt cp314 (Python 3.14) wheels.
# asyncpg<0.31 and pydantic<2.12 have no 3.14 wheels and would compile from source.
fastapi==0.138.0
uvicorn[standard]==0.49.0
sqlalchemy[asyncio]==2.0.51
asyncpg==0.31.0
pydantic==2.13.4
pydantic-settings==2.14.2
# Async AMQP client: the outbox relay publishes plain-JSON events and the
# src/consumers workers consume them (no task framework — see docs/report).
aio-pika==9.6.2
python-dotenv==1.2.2
prometheus-fastapi-instrumentator==8.0.1   # /metrics for Prometheus (RPS, status, latency)

# Development dependencies
pytest==9.1.1
pytest-asyncio==1.4.0
black==26.5.1
ruff==0.15.18
httpx==0.28.1
locust==2.44.4   # load testing (scripts/locustfile.py)

# Alembic for migrations (schema source of truth)
alembic==1.18.4
