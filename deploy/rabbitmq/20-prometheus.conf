# Expose per-queue Prometheus metrics (label `queue`) on :15692/metrics, so the Grafana
# dashboard can show the `alerts` / `scoring` queue depth building and draining. Loaded
# from conf.d alongside the image's generated default-user config.
prometheus.return_per_object_metrics = true
